<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background layer: a flat navy fill, full-bleed. The system
     masks this (circle/squircle/rounded-square/etc per launcher) so it must
     not bake in its own corner rounding - see ic_launcher_foreground.xml for
     the actual mark, kept inside the adaptive safe zone. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#10162B"
        android:pathData="M0,0h108v108h-108z" />
</vector>
