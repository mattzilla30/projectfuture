<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ProjectFutureBrowser">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Makes this app eligible as a default browser: Android offers it for http(s) links
                 and "Open with" prompts once a user has more than one such app installed. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- A fixed pool of separate OS processes for sandboxed tab engines - see
             TabEngineServiceBase's class doc (in ipc/TabEngineServiceBase.kt) for why
             a static pool rather than one process per tab, and what process isolation
             actually buys/costs here. This is the real engine for every regular tab
             MainActivity opens by default (see TabManager.newTab's doc), not a demo. -->
        <service android:name=".ipc.TabEngineService0" android:exported="false" android:process=":tabengine0" />
        <service android:name=".ipc.TabEngineService1" android:exported="false" android:process=":tabengine1" />
        <service android:name=".ipc.TabEngineService2" android:exported="false" android:process=":tabengine2" />
        <service android:name=".ipc.TabEngineService3" android:exported="false" android:process=":tabengine3" />
    </application>

</manifest>
